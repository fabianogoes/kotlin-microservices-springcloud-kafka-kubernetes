app:
  messages:
    welcome: Welcome to Order Service Local

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always