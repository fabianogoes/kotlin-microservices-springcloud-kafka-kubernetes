eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

spring:
  profiles:
    active: default
  application:
    name: order-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server